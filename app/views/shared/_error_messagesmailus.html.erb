<% if @mailer.errors.any? %>
	<div id="error_explanation">
		<div class="alert alert-error">
		The form contains <%= pluralize(@mailer.errors.count, "error") %>
	</div>
	<ul>
		<% @mailer.errors.full_messages.each do |msg| %>
		<li>* <%= msg %></li>
	    <% end %>		
	</ul>
	</div>
<% end%>
